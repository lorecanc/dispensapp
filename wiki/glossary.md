# Glossary

> Project-specific terminology. Auto-generated by wiki-indexer.

| Term | Definition | See Also |
|------|-----------|----------|
| **APIConfig** | iOS struct using `UserDefaults` to persist the backend URL, defaulting to `http://127.0.0.1:8000`. | [iOS Configuration](../config/ios-config.md), [iOS Networking](../concepts/ios-networking.md) |
| **CategoryPicker** | iOS component that maps OFF-style category keys to Italian display names (e.g. `"fresh-milk"` → `"Latte fresco"`). | [CategoryPicker](../components/ios-category-picker.md) |
| **DEFAULT_SHELF_LIFE** | Config dictionary mapping category keys (e.g. `"pasta"`, `"yogurts"`) to shelf life in days; serves as the fallback when no expiration date is provided. | [Backend Configuration](../config/backend-config.md), [Expiration Date Estimation](../concepts/expiration-estimation.md) |
| **Dispensa** | Italian for "pantry"; the name of the main inventory list tab in the iOS app. | [InventoryListView](../components/ios-inventory-list-view.md) |
| **ESTIMATED_NOTE** | Config string appended to exported markdown rows where `is_estimated` is true. | [Backend Service — Markdown Export](../modules/backend-service-markdown-export.md) |
| **EXPIRING_SOON_DAYS** | Config constant (= 3) that defines how many days before expiration an item's status shifts from "ok" to "expiring_soon". | [Item Status](../concepts/item-status.md), [Backend Configuration](../config/backend-config.md) |
| **inventoryDate** | Custom `JSONDecoder.DateDecodingStrategy` that handles ISO8601 with/without fractional seconds and SQLite-style timestamps. | [iOS Models](../concepts/ios-models.md), [iOS Networking](../concepts/ios-networking.md) |
| **InventoryItem** | The core ORM/model representing a single product in the pantry (has barcode, name, brand, expiration_date, category, quantity, etc.). | [Backend Models](../modules/backend-models.md), [iOS Models](../concepts/ios-models.md) |
| **InventoryStore** | The `@Observable` `@MainActor` class that owns all mutable state and mediates between views and the `APIClient`. | [iOS State Management](../concepts/ios-state-management.md) |
| **is_estimated** | A boolean flag on InventoryItem indicating the expiration date was auto-calculated from shelf-life defaults rather than user-supplied. | [Expiration Date Estimation](../concepts/expiration-estimation.md), [Backend Models](../modules/backend-models.md) |
| **ItemStatus** | An enum (`ok` / `expiring_soon` / `expired`) with associated colour, SF Symbol, and Italian label — used identically across backend (computed property) and iOS (native enum). | [Item Status](../concepts/item-status.md), [iOS Status Badge](../components/ios-status-badge.md) |
| **OFF (Open Food Facts)** | The public open-source food product database used for barcode lookups. | [Open Food Facts Client Service](../modules/backend-service-off.md), [Open Food Facts Integration](../concepts/off-integration.md) |
| **ScanResult** | The response payload from `POST /api/scan`, containing barcode, name, brand, categories, image URL, and found/message fields. | [Scan API](../api/scan.md), [Barcode Scan Route](../modules/backend-routes-scan.md) |
